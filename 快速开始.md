# 快速开始

普通用户只需要理解 4 个对象：

1. 公开仓库：运行时、规则、模板和文档，由项目维护者更新
2. 私有库：个人知识和项目记忆，只保存在用户选择的本地目录
3. Manager Skill：把自然语言转换成稳定的系统操作，每种受支持 AI 环境安装一次
4. 项目桥：每个项目本地生成一次，让 Agent 找到公开运行时和对应私有记忆

当前直接支持 `.agents` 和 `.claude` 两类 Skill 环境。hooks、subagents 和 MCP 都是可选适配层，不是开始使用的前提。

## 第一次安装

下载或克隆公开仓库后，只运行一次根目录安装器。

Windows：

```powershell
.\install.ps1
```

macOS / Linux：

```bash
chmod +x install.sh
./install.sh
```

安装器自动完成：

- 创建仓库内 `.venv` 并安装核心依赖
- 在公开仓库同级创建或发现 `<公开仓库名>-private`
- 初始化私有库目录、入口页、`wiki.private.json` 和私有库专属 `AGENTS.md` / `CLAUDE.md`
- 安全同步托管脚手架并初始化版本状态
- 安装 Manager Skill
- 执行 Schema 迁移、索引重建和严格 doctor

自定义私有库位置时使用：

```powershell
.\install.ps1 -PrivateRoot "<private-wiki-root>"
```

```bash
./install.sh --private-root "<private-wiki-root>"
```

## 第一个项目

在项目目录中直接说：

```text
开始工作
```

未接入时系统会自动完成一次接入；已接入时恢复上下文。也可以明确说 `将当前项目接入 wiki`。

接入结果包括：

- 忽略提交的本地 `wiki.context.json`，保存真实公开运行时和私有库路径
- `AGENTS.md` / `CLAUDE.md` 中可安全升级的托管区块
- 缺失时才创建的项目控制文件
- 私有库中的项目主干页和项目注册记录
- 项目核心脚手架版本状态

普通接入不会创建 `scripts/ai`、项目 Skill、hooks 或 subagents。只有显式启用可选适配器时才会生成这些内容。

## 日常使用

项目开发只记三句话：

- `开始工作`
- `继续`
- `收工`

资料和知识工作直接描述目标：

- `把这份资料收进当前项目`
- `把这份资料收进个人知识库`
- `基于当前项目 wiki 回答这个问题`
- `把这次稳定结论写回项目 wiki`
- `把这个跨项目经验提升成共享知识`

`收工` 会产生本地闭环回执。Agent 必须逐项处理控制文件和 wiki 回写候选，用户不需要记住回执脚本。

### 做 UI 页面时

仍然直接说目标，例如：`开始工作：重新设计订单详情页`。Agent 会自动判断任务等级：局部修改只复用当前组件和 Token；新页面或流程重做会先给出方向候选，等你用自然语言批准后再写生产代码；收工前自动检查截图、Visual QA 和无障碍证据。你可以说“用 Stitch 探索三个方向，不改代码”或“以这个 Figma 节点为已批准参考实现页面”，但不需要记任何内部脚本。

## 更新版本

已有用户直接说：

```text
更新 ObsidianToWiki
```

更新会一次完成公开 Git 预检、依赖、Skill、私有脚手架、Vault 迁移、所有已登记项目核心桥、已安装可选适配器、索引和 doctor。项目不需要逐个重新接入。

安全边界：

- 公开仓库有未提交改动时停止
- 只允许 Git fast-forward，不 stash、不 reset
- 私有托管文件有用户修改时保留原件，只生成 `.new` 候选和备份
- 项目入口只更新标记托管区块
- 项目生命周期文件有用户修改时只生成候选
- 缺失的项目控制文件可以补齐，已有文件不覆盖

高级用户可运行 `00_system/scripts/otw.ps1 update --check` 或 `00_system/scripts/otw.sh update --check` 查看报告，但这不是普通用户步骤。

## 换电脑或操作系统

公开运行时、私有库和 `wiki.context.json` 都包含本机可访问路径。新机器先运行一次安装器，再在本机首次打开的项目中说 `开始工作`。由于 `wiki.context.json` 不提交 Git，每台机器会生成自己的绑定，不应把 Windows 路径复制给 macOS/Linux。

## 暂时忽略

第一次使用不需要理解：

- 单个 Python 脚本名称
- hooks/subagents 配置
- MCP 配置
- 同步 manifest 和哈希状态文件
- 学习候选、关系同步和版本收口工具

需要深入时再阅读 [[使用手册]]、[[标准自然语言话术清单]] 和 `docs/`。
