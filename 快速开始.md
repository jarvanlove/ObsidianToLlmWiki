# 快速开始

这一页只回答 3 个问题：

1. 这套系统放在哪里
2. 第一次怎么开始
3. 日常怎么用

## 1. 这套系统放在哪里

你现在有两个仓库：

- `ObsidianToWiki`
  公开脚手架，放规则、模板、脚本和文档
- `ObsidianToWiki-private`
  私有知识库，放你的真实项目和个人知识

它们不是自动双向同步。

当前支持的是：

- 从公开脚手架同步一部分框架文件到私有库
- 私有内容不自动回写到公开仓库

## 2. 第一次怎么开始

第一次不要做太多，只做这两步。

### 步骤 A：把一个现成项目接进来

你只需要说：

- `帮我把当前项目接入 wiki`

执行后你会得到：

- 项目仓库里的 AI 工具入口
- 项目仓库里的项目控制面
- 私有知识库里的项目主干页

这些是系统内部结果，你不需要背文件名。

### 步骤 B：把一份真实资料收进来

你只需要说：

- `把这份资料收进当前项目`
- `把这份资料收进个人知识库`

执行后你会得到：

- 原始文件副本
- 来源笔记
- 更新后的索引
- 更新后的日志

只要你真的完成过这两步，这套系统就已经能用了。

## 2.1 已有用户怎么升级

已有用户不需要重新搭一套 wiki，也不需要逐个项目重新接入。升级分四层：

1. 更新公开脚手架：在 `ObsidianToWiki` 里执行 `git pull`
2. 更新本地依赖和全局管理 Skill：执行一次安装器
3. 先 dry-run，再同步私有库脚手架
4. 执行一次 hash-safe upgrade 和严格 doctor；冲突只生成 `.new` 候选

如果你正在 AI coding 工具里，可以直接用自然语言：

```text
请把当前 wiki 脚手架更新到最新版，同步到我的 private wiki，然后刷新当前项目接入并严格检查。
```

Agent 应自动完成脚本调用。脚本命令是给 agent 和高级用户看的，不是要求普通用户每天手动执行。

Windows 命令示例：

```powershell
cd <obsidiantowiki-public-root>
git pull origin main
.\install.ps1
python .\00_system\scripts\sync_private_vault.py --private-root <private-wiki-root> --dry-run --format json
python .\00_system\scripts\sync_private_vault.py --private-root <private-wiki-root>
python .\00_system\scripts\otw.py upgrade --wiki-root <private-wiki-root> --apply --all-projects
python .\00_system\scripts\otw.py doctor --wiki-root <private-wiki-root> --strict
```

macOS / Linux 命令示例：

```bash
cd <obsidiantowiki-public-root>
git pull origin main
./install.sh
./00_system/scripts/sync_private_vault.sh --private-root <private-wiki-root> --dry-run --format json
./00_system/scripts/sync_private_vault.sh --private-root <private-wiki-root>
./00_system/scripts/otw.sh upgrade --wiki-root <private-wiki-root> --apply --all-projects
./00_system/scripts/otw.sh doctor --wiki-root <private-wiki-root> --strict
```

注意：不要混用 Windows 路径和 macOS / Linux 路径。同一个项目如果是在 Windows 下接入，`wiki.context.json` 里会记录 `C:\...`；在 macOS、Linux 或 WSL 下使用时，应重新刷新接入，让上下文文件记录当前系统可访问的路径。

已经接入过的项目通常不需要手动记完整命令。打开项目后说：

```text
开始工作。
```

如果 Agent 报告了升级候选，补一句：

```text
检查升级候选，保留我的项目定制，只应用确认安全的更新。
```

## 3. 日常怎么用

项目开发只先记三句话：

- `开始工作`
- `继续`
- `收工`

### 场景一：我要开一个项目或打开一个旧项目

直接说：

- `开始工作`

预期效果：

- 未接入 wiki 的项目会先自动接入
- 已接入 wiki 的项目会恢复上下文
- 系统会做一次严格检查，确认 `wiki.context.json`、项目控制文件、脚本目录和 wiki 主干页是否齐全

### 场景二：我要继续做项目

直接说：

- `继续`

预期效果：

- 自动检查当前项目状态和本次 diff
- 结合 `TASKS.md` 选择下一步
- 不要求你记脚本名或文件名

### 场景三：我要结束这一轮

直接说：

- `收工`

预期效果：

- 查看本次 diff
- 记录验证结果
- 提醒该更新哪些项目控制文件
- 只把长期有效结论回写到 wiki

### 场景四：需求变了、出 bug 了、准备上线

直接说：

- `这是一个需求变更：<你的新需求>`
- `修复这个 bug：<现象或报错>`
- `发布前帮我检查一遍`

### 场景五：我有一份资料要收进去

直接说：

- `把这份资料收进当前项目`
- `把这份资料收进个人知识库`
- `把这张图片识别后沉淀到个人知识库`

### 场景六：我想查之前怎么做过

直接说：

- `基于当前项目 wiki 回答这个问题`
- `帮我找一下之前关于某个主题的知识`

### 场景七：我已经得到一个稳定结论，想记住

直接说：

- `把这次结论记下来`
- `把这个经验提升成共享知识`

## 你暂时可以先忽略什么

先不用强记这些高级功能：

- 学习候选
- 候选升级
- 自动发现
- 来源推荐目标
- 关系同步细节
- 版本收口工具

这些是系统增强能力，不是第一次使用的前提。

## 你下一步应该做什么

现在最对的动作不是继续读，而是：

1. 选一个真实项目
2. 让系统把这个项目接入 wiki
3. 找一份真实资料
4. 让系统把这份资料收进项目或个人知识库

然后再回来看 [[使用手册]]。
